@model IEnumerable<ServicesViewModel>
@{
    Layout = "~/Views/Shared/_HomeLayout.cshtml";
}


<partial name="_RecommentLayout" />
@* <div class="container mt-5">
    <div class="swiper-container">
        <div class="swiper-wrapper">
            @foreach (var service in Model)
            {
                <div class="swiper-slide service-item" data-category="@service.ServiceName">
                    <div class="col-md-4 d-flex align-items-stretch">
                        <div class="offer-deal text-center px-3 py-4">

                            <!-- Biểu tượng filter + loại -->
                            <div class="d-flex justify-content-between align-items-center px-3 pt-2 pb-2">
                                <div class="service-category">
                                    <i class="fa-solid fa-filter"></i>
                                    <span class="ms-2">@service.ServiceName</span>
                                </div>
                                <div class="rating">
                                    ★★★★☆
                                </div>
                            </div>

                            <!-- Hình ảnh dịch vụ -->
                            <div class="service-image">
                                <img src="@service.Thumbnail" alt="@service.ServiceName" class="img-fluid rounded">
                            </div>

                            <!-- Nội dung -->
                            <div class="service-content mt-3">
                                <h3 class="mb-2 text-primary">@service.ServiceName</h3>
                               

                                <!-- Thời gian & giá -->
                                <div class="d-flex justify-content-between align-items-center mb-3">
                                    <span class="meta-box">
                                        <i class="fa-regular fa-clock"></i> @service.Duration phút
                                    </span>
                                    <span class="meta-box fw-bold text-danger">
                                        <i class="fa-solid fa-dollar-sign"></i> @string.Format("{0:N0}", service.Price) VND
                                    </span>
                                </div>

                                <!-- Nút đặt lịch -->
                                <a href="@Url.Action("Index", "Booking", new { id = service.Id })"
                                   class="btn btn-primary w-100 py-2">
                                    Booking Now! <i class="fa-solid fa-arrow-right"></i>
                                </a>
                            </div>

                        </div>
                    </div>
                </div>
            }
        </div>

        <!-- Nút điều hướng -->
        <div class="swiper-button-prev"></div>
        <div class="swiper-button-next"></div>
    </div>
</div> *@
<partial name="_InformationHomeLayout" />
<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>

